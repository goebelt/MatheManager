## Änderungen

### 1. Sortierung nach Uhrzeit
- Termine im Dashboard werden jetzt chronologisch nach Startzeit sortiert
- Gleiches Sortierverhalten wie in der Termine-Seite

### 2. Einheitliche Konfliktlogik
- Eigene `getConflictStatus`-Funktion in DayView entfernt (fehlerhaft: sortierte nach `new Date(a.date)` statt nach Uhrzeit)
- Stattdessen `getAppointmentStatus` aus `lib/scheduling.ts` verwendet – identische Logik wie in der Termine-Seite
- Stornierte Termine werden korrekt ignoriert

### 3. Tests aktualisiert
- 5 neue Tests für DayView:
  - Sortierung nach Uhrzeit (unabhängig von Eingabereihenfolge)
  - Konflikt-Erkennung bei Überlappungen
  - Knapp-Indikator bei ≤5 Min Pause
  - Kein Indikator bei ausreichendem Abstand
  - Kein Konflikt bei stornierten Terminen
- Alle 216 Tests grün ✅

### 4. README aktualisiert
- Dashboard: Wochenübersicht → Tagesübersicht
- Sortierung und Konfliktlogik dokumentiert
- DayView-Kommentar in Projektstruktur aktualisiert

## Geänderte Dateien
- `src/components/DayView.tsx`
- `src/components/__tests__/DayView.test.tsx`
- `README.md`
