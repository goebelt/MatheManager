/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  
  // Optional: Custom domain oder andere Next.js Einstellungen
  experimental: {
    // Neue Experimente können hier aktiviert werden
  },
};

module.exports = nextConfig;